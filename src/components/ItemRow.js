import React from "react";
import PropTypes from "prop-types";

const ItemRow = (props) => {
  return (
    <div>
      <h1>Employs list</h1>
    </div>
  );
};

ItemRow.propTypes = {};

export default ItemRow;
